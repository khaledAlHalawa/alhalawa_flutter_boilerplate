part of 'app_theme.dart';

final ColorScheme _darkColorScheme =
    ColorScheme.fromSwatch(primarySwatch: AppColors.dark);

final ColorScheme _lightColorScheme =
ColorScheme.fromSwatch(primarySwatch: AppColors.primary);
